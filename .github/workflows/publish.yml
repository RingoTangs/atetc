name: Publish to npm

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish package
    runs-on: ubuntu-latest
    environment: npm-publish

    steps:
      - name: Check out release tag
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.23.2
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate release metadata
        id: metadata
        shell: bash
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          RELEASE_PRERELEASE: ${{ github.event.release.prerelease }}
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          EXPECTED_TAG="v${VERSION}"

          if [[ "$RELEASE_TAG" != "$EXPECTED_TAG" ]]; then
            echo "Release tag $RELEASE_TAG does not match package version $EXPECTED_TAG" >&2
            exit 1
          fi

          if [[ "$VERSION" == *-* ]]; then
            if [[ "$RELEASE_PRERELEASE" != "true" ]]; then
              echo "Prerelease version $VERSION must use a GitHub prerelease" >&2
              exit 1
            fi
            PRERELEASE="${VERSION#*-}"
            NPM_TAG="${PRERELEASE%%.*}"
            case "$NPM_TAG" in
              alpha | beta | rc) ;;
              *)
                echo "Unsupported npm prerelease tag: $NPM_TAG" >&2
                exit 1
                ;;
            esac
          else
            if [[ "$RELEASE_PRERELEASE" == "true" ]]; then
              echo "Stable version $VERSION must not use a GitHub prerelease" >&2
              exit 1
            fi
            NPM_TAG="latest"
          fi

          PACKAGE_NAME="$(node -p "require('./package.json').name")"

          echo "package_name=$PACKAGE_NAME" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "npm_tag=$NPM_TAG" >> "$GITHUB_OUTPUT"

      - name: Check package
        run: pnpm check

      - name: Build package
        run: pnpm build

      - name: Preview package contents
        run: pnpm pack:check

      - name: Install OIDC-compatible npm
        run: npm install --global "npm@>=11.5.1 <12"

      - name: Check npm registry
        id: registry
        env:
          PACKAGE_NAME: ${{ steps.metadata.outputs.package_name }}
          VERSION: ${{ steps.metadata.outputs.version }}
        run: |
          if npm view "$PACKAGE_NAME@$VERSION" version --prefer-online >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish package
        if: steps.registry.outputs.published != 'true'
        # 检查、构建和打包预览已经在前序步骤完成，避免再次触发 prepublishOnly。
        run: npm publish --ignore-scripts --access public --tag "${{ steps.metadata.outputs.npm_tag }}"
